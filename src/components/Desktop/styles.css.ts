import { style } from '@vanilla-extract/css'

export const container = style({
    backgroundColor: '#000',
    bottom: 0,
    top: 0,
    left: 0,
    right: 0,
    position: 'fixed',
    width: '100vw',
    height: '100vh',
})
